# 经验记录

- 需求建模在早期会快速变化，先用当前仓库的真实语义校正概念，再决定是否保留或删除中间层级；不要过早把 `question_group/question` 一类临时结构写死。
- 当用户明确区分产品概念与底层实现概念时，UI 命名和数据模型都要跟着分离；这里应把 `workspace` 视为底层实现细节，而不是继续暴露给用户。
- 出现异常混杂文本时，立即停止复用有污染风险的提问形式，改用纯文本直接问答，避免把噪声误记为正式需求。
